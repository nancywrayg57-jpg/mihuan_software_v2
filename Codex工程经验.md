---
title: Codex工程经验（蜜獾官网）
date: 2026-07-12
source_project: 蜜獾官网 / Honey Badger Corporate Website
baseline:
  - Agent.md
  - docs/requirements_v2.md
  - UI/
  - docs/生产物料清单.md
  - docs/蜜獾官网页面文案.md
tags:
  - Codex
  - 企业官网
  - 多语言
  - 前端工程
  - 视觉落地
  - 部署验证
---

# Codex工程经验（蜜獾官网）

本文承接既有 Codex 工程经验，并针对 **蜜獾官网** 做项目化收敛。它是执行经验文件，不是宪法；任何冲突均以根目录 `Agent.md` 为准。

---

## 1. 工作方式

### 1.1 先读后写

每次动手前先读 `Agent.md`、`docs/requirements_v2.md` 和任务相关文件。当前项目同时存在旧项目模板、官网需求、现有 UI 与生产物料/页面文案，最常见风险是把旧项目事实或历史占位数据带入生产文档/代码。

### 1.2 不继承旧项目事实

旧基线文档中的仓库、账号、代理销售平台、资金链路、RDS/Redis/OSS、供应商适配等内容只能作为治理方法参考，不能作为蜜獾官网事实。蜜獾官网当前是企业展示型站点，除非管理员新增需求，不默认存在订单、支付、会员、后台 CMS 或数据库。

### 1.3 诊断优先于修复

问题处理顺序固定：复现现象 -> 读取日志/构建输出 -> 定位根因 -> 提出最小修复 -> 执行验证。连续两次修复失败后停止修改，输出根因假设和下一步需要的证据。

### 1.4 每次交付要可追溯

交付至少说明：改了哪些文件、基于哪些输入、做了哪些验证、哪些信息仍是占位或待管理员确认。没有验证证据的“已完成”只能算草稿。

---

## 2. 需求与原型落地经验

### 2.1 `docs/requirements_v2.md` 是业务 SSOT

五个页面、三语支持、导航语言切换、面包屑、客服悬浮、ICP备案占位、HTTPS 与 Let's Encrypt 自动续签都来自需求文件。实现时若发现现有 UI 或生产物料缺失某一项，以需求为准补齐。

### 2.2 当前 UI 与生产资料是设计实现参考

当前可用参考源包括：

1. `UI/`：现有静态页面、三语页面、共享脚本与样式。
2. `UI/assets/styles.css`：颜色、字体、栅格、间距、组件和响应式规则的实际实现。
3. `docs/生产物料清单.md`：域名、部署、客服、素材和上线状态源。
4. `docs/蜜獾官网页面文案.md`：三语页面内容、产品/服务口径和 SEO 文案源。

这些文件是当前设计和实现参考。不得再把不存在的旧生成器目录或旧分析目录作为必读、设计基线或生产事实来源；示例联系方式、占位 ICP、示例年份、假图片地址和未确认授权表述仍需按生产物料清单校准。

### 2.3 ZennoLab 关系表达要克制

需求确认的是“蜜獾公司是俄罗斯 `zennolab` 公司在中国的运营实体”。除非管理员书面确认，不要使用“唯一”“独家”“总代理”“官方唯一”等可能扩大法律含义的描述。

---

## 3. 前端工程经验

### 3.1 初始化工程时先定边界

如果仓库尚未初始化应用工程，先确认框架、包管理器、部署目标和静态/动态内容边界。不要在文档任务中顺手生成应用骨架；不要在 UI 任务中顺手改部署脚本。

### 3.2 TypeScript 与请求层

正式前端工程应启用 strict TypeScript 并禁用 `any`。凡有 HTTP 请求，统一封装响应检查：

1. 检查 `response.ok`。
2. 检查 `content-type` 是否符合预期。
3. 对 HTML 错误页、登录页、Nginx 404/413 返回明确错误。
4. 错误信息包含 HTTP 状态码和接口路径。

### 3.3 不做前端假状态

官网大部分内容可以是静态展示；但招聘投递、在线客服、新闻订阅、联系表单等一旦表现为“提交成功”，必须有真实后端或第三方服务支撑。没有真实接口时，使用明确的联系渠道跳转，不模拟成功。

### 3.4 多语言不要事后补丁

中文、英文、俄语要从信息架构阶段同步设计。导航、面包屑、SEO title、description、按钮、页脚、客服入口、错误提示都必须有三语 key。俄语文本更长，布局要预留宽度和换行策略。

### 3.5 UI 稳定性

多语言切换、移动端导航、客服悬浮、卡片列表和新闻标题是最容易溢出的区域。用稳定尺寸、`minmax()`、明确断行和响应式布局解决，不用 viewport 字号缩放或动态拼 Tailwind 类名。

---

## 4. 视觉落地经验

### 4.1 保留工业可靠感

视觉方向是企业级、现代、可信，不是营销落地页。首页第一屏要让用户立即知道 Honey Badger 与 ZennoLab 的关系，以及官网提供什么产品/服务入口。

### 4.2 设计令牌优先

优先复用设计系统中的蓝色交互体系、白/浅灰背景、Inter/Open Sans/Noto Sans SC 字体、1200px 内容宽度、76px Header、8px spacing rhythm。不要每个页面重新发明颜色和圆角。

### 4.3 图片和资产要可授权

原型中的远程图片可用于理解风格，不能直接热链进生产。生产图片需要来自：

1. 管理员提供的品牌资产。
2. 项目本地授权素材。
3. 明确许可的图库或自生成资产，并保存授权/来源说明。

### 4.4 客服悬浮不要遮挡内容

客服按钮在所有页面常驻，但不能遮挡移动端底部主要 CTA、表单按钮或页脚备案信息。需要覆盖桌面 hover、移动端点击展开、键盘 focus 和关闭行为。

---

## 5. 部署与上线经验

### 5.1 HTTPS 不是“最后一步”

域名 `www.honeybadgersoft.com`、Let's Encrypt、自动续签、HTTP -> HTTPS、www/non-www 策略、静态资源缓存和 404 回退应在部署方案中一起设计。不要等页面完成后才补证书。

### 5.2 上线检查以公网为准

本地 build 成功不代表上线成功。正式验收至少检查：

1. 正式域名可访问。
2. 证书链有效，自动续签任务存在。
3. 首页与核心页面直接刷新不 404。
4. 三语切换可用且 URL/状态稳定。
5. 静态资源无 404、无混合内容。
6. 客服入口在桌面与移动端可见。
7. 页脚 ICP 位置和版权位置存在。

### 5.3 配置不要写死

域名、邮箱、客服账号、备案号、统计脚本、CDN 地址、API Base 都必须可配置。真实值未确认时使用明确占位，不编造。

---

## 6. Windows 与编码经验

### 6.1 中文文件名要注意编码

本项目包含中文文件名。PowerShell 读取/输出中文 Markdown 时优先设置 UTF-8 输出编码，避免把有效中文误判成乱码：

```powershell
[Console]::OutputEncoding=[System.Text.Encoding]::UTF8
Get-Content -Encoding UTF8 -Raw <path>
```

### 6.2 二进制不要用文本方式处理

`screen.png`、图片素材、压缩包和构建产物不要用文本重定向写入。图片检查优先用浏览器、截图或二进制安全的复制方式。

### 6.3 路径与工具链

Node、Vite、Playwright、压缩工具在中文路径、空格路径、长路径下更容易出问题。遇构建失败先排查路径、依赖版本和编码，再怀疑业务代码。

---

## 7. 验证建议

### 7.1 文档类任务

检查：

1. 文件是否 UTF-8 可读。
2. 是否覆盖需求中的页面、语言、客服、备案、HTTPS。
3. 是否清除了旧项目不适用事实。
4. 是否未修改任务外文件。

### 7.2 前端类任务

常规命令按项目实际脚本执行，通常包括：

```powershell
pnpm typecheck
pnpm lint
pnpm test
pnpm build
```

若项目使用 workspace，则按实际脚本调整。不要为了通过验证而删除测试或降低 strict 配置。

### 7.3 浏览器验收

至少覆盖：

1. 桌面宽屏。
2. 平板宽度。
3. 移动端宽度。
4. 中文、英文、俄语。
5. 首页、产品、新闻、招聘、关于我们。
6. 页面刷新、导航跳转、面包屑、客服展开。

---

## 8. 文档与交接

### 8.1 经验沉淀要收敛

聊天记录不是长期事实源。重大架构选择、部署方式、接口契约、内容模型和视觉规则应沉淀到项目文档。若当前任务明确禁止修改其他文件，则不得额外写入沉淀文档，需在交付说明中标明。

### 8.2 `<Handoff_State>` 要具体

跨 Agent 交接不能只写“继续开发”。至少包含目标、已完成、变更文件、验证结果、产物、敏感边界、风险、下一步和回滚方式。

```xml
<Handoff_State>
Target:
Completed:
Changed_Files:
Validation:
Artifacts:
Sensitive_Boundary:
Open_Risks:
Next_Steps:
Rollback:
Context_For_Next_Agent:
</Handoff_State>
```

---

## 9. 快速检查清单

- [ ] 是否先读了 `Agent.md`、`docs/requirements_v2.md` 和相关原型？
- [ ] 是否避免继承 ZennoProxy 旧项目事实？
- [ ] 是否覆盖首页、产品、新闻、招聘、关于我们五页？
- [ ] 是否实现或规划三语、面包屑、语言切换、页脚、客服悬浮？
- [ ] 是否没有使用原型里的假联系方式、假 ICP、假年份和远程热链图片？
- [ ] 是否保证 ZennoLab 关系表述不过度？
- [ ] 是否检查桌面/移动端文本溢出？
- [ ] 是否完成 typecheck/lint/test/build 或明确说明未验证？
- [ ] 是否只修改任务要求文件？
