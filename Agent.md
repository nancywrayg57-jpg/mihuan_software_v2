# Agent.md - 蜜獾官网项目宪法

> **版本**：v1.0
> **项目**：蜜獾官网（Honey Badger Corporate Website）
> **路径**：`workspace\mihuan_software`
> **项目类型**：企业官网独立站 / 前台展示型网站
> **上线域名**：`https://www.honeybadgersoft.com/`
> **基线输入**：`docs/requirements_v2.md`、`UI/`、`docs/生产物料清单.md`、`docs/蜜獾官网页面文案.md`、根目录 `Codex工程经验.md`
> **最后更新**：2026-07-12

---

## 零、最高优先级规则

1. **项目根目录 `Agent.md` 是唯一 Agent 宪法文件**。任何旧项目宪法、聊天记录、生成器 HTML、外部模板都不能覆盖本文规则。
2. 每次新会话必须先读取：
   - `Agent.md`
   - `Codex工程经验.md`
   - `docs/蜜獾官网项目需求分析.md`
   - 与任务相关的 `UI/`、`docs/生产物料清单.md`、`docs/蜜獾官网页面文案.md` 文件
3. 修改已有逻辑前必须先通过 `rg`、AST、局部源码读取或差异检查了解上下文，禁止凭记忆盲改。
4. 任务只允许修改业务目标明确要求的文件。无业务需求不得重构、格式化或批量改动无关文件。
5. Bug 修复必须遵循：分析日志/现象 -> 定位根因 -> 提出方案 -> 最小改动 -> 验证闭环。连续 2 次修复失败立即停止并输出根因假设。
6. 高危操作（数据库、部署、批量删除、证书替换、生产配置变更）执行前必须显式说明风险、范围、回滚方式。
7. 若本文与 `Codex工程经验.md` 冲突，以本文为准；若需求与实现冲突，以 `docs/requirements_v2.md` 为当前业务 SSOT，并反向更新方案。

---

## 一、项目定位

蜜獾官网是蜜獾公司正式对外发布的企业官网独立站，用于展示公司形象、产品能力、企业背景、新闻资讯和招聘信息。

官网必须明确表达：**蜜獾公司是俄罗斯 `zennolab` 公司在中国的运营实体**。除非管理员提供法律/品牌确认，不得自行扩写为“唯一代理”“独家授权”“官方总代理”等更强表述。

当前项目范围：

1. 首页
2. 产品介绍
3. 新闻资讯
4. 人才招聘
5. 关于我们
6. 全局页头、页脚、面包屑导航、语言切换、在线客服悬浮入口
7. 部署配置、HTTPS、三语内容清单、上线检查清单

当前不在范围内：

1. 会员系统、订单系统、支付系统、库存系统
2. 后台 CMS、招聘投递后台、在线客服工作台
3. 未确认的 ZennoLab 授权文案、ICP备案号、联系方式、邮箱账号密钥

如后续新增后台、表单、CMS 或接口能力，必须先补充接口契约和安全边界，再实现。

---

## 二、单一事实源

| 类型 | SSOT |
|---|---|
| 官网业务需求 | `docs/requirements_v2.md` |
| 视觉方向与设计令牌 | `UI/assets/styles.css`、`docs/生产物料清单.md` |
| 页面实现与文案参考 | `UI/`、`docs/蜜獾官网页面文案.md` |
| Agent 行为规范 | 根目录 `Agent.md` |
| Codex 工程经验 | 根目录 `Codex工程经验.md` |

当前 `UI/` 目录和生产文档是现有设计与实现参考源。落地实现时必须以现有页面结构、样式令牌、三语文案和生产物料状态为准，不得盲目继承旧原型或外部模板：

1. 不得把示例联系方式、QQ群号、邮箱、年份、ICP备案号当作真实信息。
2. 不得直接热链原型中的外部图片地址，生产素材必须来自授权资产、项目本地资产或管理员确认的 CDN。
3. 不得把原型中的英文/中文混排文案视为最终三语内容。
4. 不得把静态 HTML 原型中的假按钮、假表单、假客服状态当作已实现功能。

---

## 三、已确认业务要求

### 3.1 域名与安全

1. 官网正式域名：`https://www.honeybadgersoft.com/`
2. 启用 Let's Encrypt 证书。
3. 证书必须支持自动续签。
4. 域名、API Base、资源 CDN、邮箱域名不得写死在不可配置代码中。

### 3.2 品牌与内容

1. 整体风格参考 `zennolab.com`，但必须形成蜜獾官网自己的企业表达。
2. LOGO 可在现有 LOGO 基础上重设计；未获确认前不得覆盖原始品牌资产。
3. 支持中文、英文、俄语三种语言。
4. 三语内容必须完整对齐，禁止某一语言缺失时静默回退为乱码、空白或混合语言。

### 3.3 页面与模块

1. 首页：显著说明蜜獾与 ZennoLab 的中国运营实体关系。
2. 产品介绍：产品文案、素材展示、能力说明。
3. 新闻资讯：企业活动、行业新闻。
4. 人才招聘：岗位、团队价值、招聘流程、联系方式。
5. 关于我们：品牌介绍、定位与形象、企业背景、公司主体说明、联系我们。

### 3.4 全局站点要求

1. 桌面端与移动端自适应。
2. 统一页头导航与页脚。
3. 每个页面在页头导航下方提供面包屑导航。
4. 语言切换放置在导航栏。
5. 页脚预留 ICP 备案信息位置和版权信息位置；无真实备案号时只能显示占位说明。
6. 全局在线客服悬浮按钮在桌面端、移动端所有页面常驻。
7. 客服入口提供微信、QQ、钉钉三种联系路径；未确认真实账号前不得填入示例号。
8. 对外邮箱使用阿里云企业邮箱；账号、SMTP 密钥、后台凭据不得入仓。

---

## 四、视觉与交互基线

来自 `UI/assets/styles.css`、`docs/生产物料清单.md` 与 `docs/蜜獾官网页面文案.md` 的基线约束：

1. 品牌关键词：Industrial Reliability、Corporate / Modern、authoritative、resilient、transparent。
2. 视觉基调：白色与浅灰为主体，高对比蓝色用于交互，绿色/紫色/青色等作为功能强调色。
3. 主要字体：标题/导航使用 Inter，正文使用 Open Sans，中文使用 Noto Sans SC 兜底。
4. 页面栅格：12 列流式网格，内容最大宽度约 1200px，8px spacing rhythm。
5. Header 高度：76px。
6. Section 间距：桌面约 100px，平板约 80px，移动约 60px。
7. 客服悬浮入口：右下角常驻，桌面 hover 可展开微信、QQ、钉钉标签，移动端需可触达且不遮挡主要内容。
8. 卡片与按钮必须有稳定尺寸和响应式约束，避免语言切换后文本溢出或布局跳动。

落地时必须遵守：

1. 禁止动态拼接 Tailwind 类名，例如 `grid-cols-${count}`；动态布局使用内联 style、CSS 变量或显式 class map。
2. 禁止使用纯装饰性大渐变、无意义光斑或营销感过强的空洞 hero；官网第一屏必须直接传达 Honey Badger / ZennoLab 中国运营实体关系。
3. 按钮、导航、语言切换、客服入口必须具备清晰 hover/focus/active 状态。
4. 多语言文本不得用 viewport 宽度缩放字体；通过合理断行、容器宽度、字号层级和内容重写解决。
5. 页面不允许出现重叠、遮挡、不可点击、横向滚动等基础响应式问题。

---

## 五、技术与实现红线

### 5.1 前端

1. 若初始化正式前端工程，必须启用 TypeScript strict，禁用 `any`。
2. 组件不得硬编码会变化的业务字段、域名、联系方式、备案号和外部链接。
3. 涉及 Dialog/Modal 的编辑类交互必须由外层传入 `key` 强制重挂载。
4. 涉及防抖、节流、异步回调、版本号或最新状态读取的 Hook，必须用 `useRef` 或等价机制规避闭包陷阱。
5. 所有日期、新闻发布时间、版权年份不得直接把原生 Date 对象渲染到模板，必须经统一格式化工具处理。
6. 如使用 fetch/HTTP 客户端，必须检查 `response.ok` 与 `content-type`；遇 HTML 错误页等非预期 JSON 响应时抛出包含完整 HTTP 状态码的明确错误。
7. 涉及数据变更的操作必须调用真实后端 API；没有后端支撑时只能做静态联系方式跳转或禁用提交，禁止纯前端假成功。

### 5.2 多语言

1. 三语内容建议使用稳定 key 管理：`zh-CN`、`en-US`、`ru-RU`。
2. 每个页面的 title、description、导航、面包屑、按钮、页脚、客服入口均需三语覆盖。
3. 语言切换不得丢失当前页面上下文。
4. 若生成静态页面，需保留 SEO 可识别的语言路径、`hreflang` 或等价方案。
5. 品牌名、产品名可保留英文，但说明性文案必须按当前语言完整本地化。

### 5.3 后端/API（仅在后续引入时适用）

1. 所有跨层调用首参数必须携带上下文对象（如 Go 的 `ctx context.Context` 或 Node 请求上下文）。
2. Node.js 服务必须启用严格 TypeScript，禁用 `any`。
3. CRUD 查询与更新如涉及租户、组织、部门或内容管理权限，必须无条件携带 `tenant_id`、`org_id` 或等价权限边界，禁止仅凭主键操作。
4. 复杂流程必须使用状态机，禁止在业务逻辑中硬编码 `status = 1` 类状态跳转。
5. 数据库结构变更必须使用时间戳增量迁移脚本，禁止直接覆写原始 schema。
6. 如未来引入资金、库存、订单或积分，必须使用高精度 decimal 与乐观锁/Redis 分布式锁；禁止 Float/Double 和无锁内存计算。

---

## 六、安全与合规

1. 严禁提交任何密钥、邮箱密码、服务器凭据、证书私钥、客服账号后台密码、生产 `.env`。
2. `.env.example` 只能包含变量名和脱敏示例。
3. 生产验证日志必须脱敏，不输出 token、密码、完整手机号、密钥、邮箱登录凭据。
4. ZennoLab 关系、授权范围、商标表达必须使用管理员确认文案。
5. 新闻资讯、企业活动、招聘信息如涉及真实人物、客户、案例或图片，必须确认授权。
6. ICP 备案号未确认前只能预留位置，不得编造。

---

## 七、Git 与协作规则

1. Git 操作前必须确认当前目录是项目真实 repo root、remote 指向预期仓库、HEAD 为预期基线、`git status` 无非预期变更。
2. 未经管理员明确要求，不得执行 `git reset --hard`、`git checkout --`、强推、批量删分支等破坏性操作。
3. 不得修改用户未要求的文件；发现脏工作树时，先区分用户变更与本次变更，绝不回滚他人改动。
4. PR 或跨 Agent 交接必须包含 `<Handoff_State>`。
5. 同一接口或逻辑变更发生 Agent 间协商冲突超过 3 次，立即输出 `:red_circle: [FATAL - HUMAN_INTERVENTION_REQUIRED]` 并停止推进。

`<Handoff_State>` 模板：

```xml
<Handoff_State>
Target:
Completed:
Changed_Files:
Validation:
Artifacts:
Sensitive_Boundary:
Open_Risks:
Next_Steps:
Rollback:
Context_For_Next_Agent:
</Handoff_State>
```

---

## 八、验证闭环

交付前至少执行与变更相匹配的验证：

1. 文档变更：检查文件编码、标题、链接路径、需求覆盖、旧项目事实是否清除。
2. 前端变更：运行 typecheck、lint、test、build；用浏览器验证桌面与移动端。
3. UI 变更：截图检查首页、产品、新闻、招聘、关于我们，以及中/英/俄语言切换。
4. 部署变更：验证 HTTPS、证书续签配置、域名跳转、缓存策略、404 页面、静态资源。
5. 接口变更：验证 2xx/4xx/5xx、非 JSON 响应防御、超时和错误提示。

若受环境限制无法本地执行端到端验证，最终结果必须明确标注 `[未经本地验证]`，不得声称测试通过。

---

## 九、紧急熔断

出现以下情况立即停止：

1. 连续 2 次修复失败。
2. 同一接口或逻辑变更 Agent 间协商冲突超过 3 次。
3. 发现凭据、证书、真实用户数据或未授权品牌资产可能入仓。
4. 发现 ZennoLab 关系表述存在法律/品牌风险且无管理员确认。
5. 生产部署、证书、DNS 或服务器变更缺少回滚方案。

输出格式：

```text
:red_circle: [FATAL - HUMAN_INTERVENTION_REQUIRED]
分歧点/阻塞点：
已验证事实：
根因假设：
需要人类裁决：
```

---

## 十、快速检查清单

- [ ] 是否读取了 `Agent.md`、`Codex工程经验.md`、`docs/蜜獾官网项目需求分析.md` 和任务相关的 `UI/`、生产物料、页面文案文件？
- [ ] 是否只修改任务要求的文件？
- [ ] 是否移除了旧项目仓库、服务器、资金链路、代理销售平台等不适用于官网的事实？
- [ ] 是否保留蜜獾官网五个页面、三语、面包屑、语言切换、客服悬浮、ICP备案占位、HTTPS 要求？
- [ ] 是否避免把原型中的示例联系方式、图片热链、年份、备案号当作真实数据？
- [ ] 是否保证 ZennoLab 关系表述不越权？
- [ ] 是否完成必要验证，或明确标注 `[未经本地验证]`？
